application:
  secret-key-jwt: 

  database:
    host: 127.0.0.1
    port: 5432
    db-user: postgres
    db-password: postgres
    db-name: cleverbank
    #db-ssl-mode: '${DB_SSL_MODE}' # disable,require,verify-ca,verify-full